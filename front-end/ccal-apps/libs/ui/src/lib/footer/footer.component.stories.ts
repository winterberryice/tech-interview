import { FooterComponent } from './footer.component';

export default {
  title: 'FotterComponent',
};

export const primary = () => ({
  moduleMetadata: {
    imports: [],
  },
  component: FooterComponent,
  props: {},
});
